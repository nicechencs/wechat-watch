# Copy to /home/box/.local/share/wechat-persist/watch/webhook.env
# chmod 600 that file. Never commit real values.
WX_WATCH_WEBHOOK_URL=
WX_WATCH_WEBHOOK_KEY=
# Optional. Default Authorization (Bearer <key>).
# WX_WATCH_WEBHOOK_AUTH_HEADER=Authorization
